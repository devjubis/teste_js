// String, number, undefined, null, boolean, (symbol)  
const nome = 'Luiz' // String 
const nome1 = "Luiz" // String 
const nome2 = `Luiz` // String 

const num1 = 10 // Number
const num2 = 10.52 // Number 

let nomeAluno; // undefined (valor indefinio) -> Declarou a variável mas não inicializou ela 
const sobrenomeAlubo = null // Nulo -> Não apontam pra lugar nenhum na memória  
/* 
 * Diferença entre null e udenfined é que quando se utiliza nulo, eu explicitamente vou lá e aponto
 * que essa variável não tem lugar nenhum na memória.
 * Quando é uma variave undefined, eu nunca vou criar essa variavel e setar o valor dela para 
 * undefined, como consigo fazer com null. Quem faz isso é a linguagem JavaScript. 
*/ 

const aprovado = true // Boolean (Valor Lógico) 
const reprovado = false // Boolean (Valor Lógico)



